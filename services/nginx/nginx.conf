upstream app {
	server app:5000;
}

server {
	listen 80;

	location / {
		root /usr/nginx/files;
		try_files $uri $uri/ =404;
	}

	location /api {
		proxy_pass http://app;
		proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
		proxy_set_header Host $host;
		proxy_redirect off;
	}
}
